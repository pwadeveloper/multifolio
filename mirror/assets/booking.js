(async()=>{const message=document.getElementById('booking-status');try{const response=await fetch('/api/payment-config',{cache:'no-store'});if(!response.ok)throw new Error();const data=await response.json();if(data.bookingUrl){const link=document.getElementById('booking-link');link.href=data.bookingUrl;link.hidden=false;message.textContent='Find a time that works for you.';}else{message.textContent='Intro-call scheduling will be available here soon.';}}catch{message.textContent='Scheduling is temporarily unavailable. Please try again later.';}})();
